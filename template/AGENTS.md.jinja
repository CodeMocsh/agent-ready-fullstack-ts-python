# Project Instructions for AI Agents

Instructions for AI coding agents working on {{ package_name }}. Follows the
[AGENTS.md](https://agents.md) convention.

This file is the principles, the map, and the index. Everything else is one link away — so a
new rule belongs here only if it is a principle, and anything with detail in it goes in `docs/`
with a pointer from here.

## Approach

You are a principal engineer. You care about the shape of the system over the long term,
not only whether the tests pass. Elegance is less code doing more: every line must earn
its keep.

Resolve ambiguity before building, not after. A request you could satisfy two different
ways is a request you do not understand yet. Ask, or state the assumption you are
proceeding on and why. Guessing and building is the expensive failure; asking costs one
round.

Code is the source of truth for behaviour; docs are the source of truth for intent. When
they disagree the doc is wrong. Fix the doc, not the code.

Tests validate outcomes, not implementation. A test earns its keep by failing when
behaviour breaks: one that mirrors the implementation only makes refactoring expensive,
and one that cannot fail is dead weight. Cover the seams and the edges that actually
bite. Coverage percentage is not the goal, and deleting a test that no longer
distinguishes anything is a real improvement.

## Fail loudly

No code path continues past a condition it did not plan for. Four bans, and both halves
are in scope:

- No `except Exception`, and no `catch` that continues.
- No default standing in for a failure — no `or {}`, no `?? []`, no quietly returned
  `None` or `undefined`.
- No failure signalled by a return value a caller can drop. If continuing would be
  wrong, raise or throw.
- No warning where the code cannot correctly proceed.

Not crashing is legitimate only when all three of these hold: the design plans for the
condition, the contract names it, and the code reports it. Fewer than three, and it
raises.

This is not a style preference, because in an app with two halves **a silent failure
looks like an empty screen**. A route that swallows a store error answers `200 []` and
the table renders "No tasks yet". A `catch` around a mutation leaves the optimistic
update on screen and the write on the floor. A mock handler that answers a case the
backend refuses makes the contract suite green against a shape the real service never
returns. Every one of those reads as working software — and a test that asserts the
*absence* of an effect passes just as happily against the bug. Assert the failure
itself.

The contract is where this is cheapest to get right: declare every status code a route
can return, with a model for anything that has a body, and the other half then cannot
fail to handle it without a type error.

## Zero comments

No explanatory comments, no docblocks, no TODO/FIXME notes, no lint or type suppression
directives (`biome-ignore`, `@ts-expect-error`, `@ts-ignore`, `# noqa`, `# type: ignore`),
no commented-out code. Shebangs and TypeScript `///` directives are executable
directives, not comments.

Express intent through names, structure, types, and tests. Rationale goes in the commit
message; a decision goes in `docs/adr/`. This relocates rationale rather than removing it,
so a repo that adopts the rule and still writes `fix: bug` has simply deleted the
explanation.

**An ADR cites nothing by section number.** Name the thing — the route, the function,
the rule, the invariant — and let the reader grep. A section number points into one
revision of one document, and an ADR outlives the document whose structure it borrowed.

One carve-out: a published public API. Where a package is consumed outside this repo its
exported surface carries JSDoc or a docstring, because that text is shipped documentation,
not an explanation aimed at a reader of the source. Everything internal stays bare. Such a
doc states contracts, not reasoning: behaviour, failure, timing, ownership, safe use. Link
the rationale; don't restate it.

Scope: source, tests, scripts. Config files may carry comments where the format offers no
other way to explain a rule, and that includes `frontend/*.config.ts`, which is
configuration that happens to be written in TypeScript. **Vendored and generated code is
out of scope entirely.**

**The rule is enforced, because on its own it does not hold.** `make lint` fails on any
comment token under `frontend/{src,tests,e2e,devtools}` and `backend/{app,tests,devtools}`
— `frontend/devtools/comments.mjs` and `backend/devtools/comments.py` are the two gates.
An agent reads this file at the top of a session and explains in place anyway, because
that is what the training data does. The suppression half is the half most worth
mechanising: refusing the spelling turns a threshold decision taken silently at the point
of pain into either a fix in the code or a reviewable line in `biome.json`,
`tsconfig.json` or `pyproject.toml`.

## Simplified technical English

Every word a human reads is written the way ASD-STE100 says to write a maintenance manual: one
idea per sentence, active voice, and one meaning per term. That covers prose and code alike —
docs, commit messages, decisions, identifiers, test names, log lines, and the message a failure
carries.

**Take the rules, not the dictionary.** ASD-STE100 ships a controlled vocabulary chosen for
aircraft maintenance, and this repo has its own nouns. `CONTEXT.md` is the word list that binds
here: one term per concept, and that term every time the concept appears — in a sentence, in a
symbol name, in the text of an error.

This is what makes *Zero comments* affordable. With no comment to fall back on, the name and the
failure message are the whole explanation, so they are worth the care the code gets.

## Layout

Two halves and a root layer. Each half installs, lints, tests and builds without the
other's toolchain: `pnpm` runs in `frontend/`, `uv` in `backend/`, `make` at the root fans
out to both. There is no root `package.json`, and `pnpm install` at the root is meant to
fail.

```
openapi.json          the contract -- generated from the backend, committed
Makefile              the interface across both halves
CONTEXT.md            the vocabulary
docs/adr/             decisions that would otherwise look like they could be simpler

frontend/             everything pnpm touches
  src/api/            the contract: base.ts, client.ts, types.ts, schema.ts (generated)
  src/mocks/          the fake implementation of that contract -- permanent, not a scaffold
  src/components/     UI; ui/ is vendored shadcn
  src/index.css       the @theme block -- see the theme rule below
  src/router.tsx      routes
  tests/              mirrors src/
  e2e/                Playwright; *.live.spec.ts needs a running backend
  devtools/           the frontend's own gates, and the scanner they share

backend/              everything uv touches
  app/main.py         builds the FastAPI app
  app/models.py       the pydantic models the contract is made of
  app/routes.py       the endpoints
  app/deps.py         what a route is handed
  app/wiring.py       the only reader of the environment
  app/serve.py        the one-origin entrypoint: app.main plus the frontend bundle
  app/identity.py     which tenant a request is -- ships as a stub returning "default"
  app/store/          the data layer; ddl.py is the schema, as data
  tests/              mirrors app/, except tests/integration/, which is a tier
  tests/tiers.py      what a tier needs and what runs it
  tests/test_gate.py  tests the repository rather than the app

deploy/               schema.sql and roles.sql -- generated from app/store/
```

## The rules that bite

Each of these is something a reasonable edit undoes without anything failing until later.

**The contract**

- `openapi.json` and `frontend/src/api/schema.ts` are generated from
  `backend/app/{models,routes}.py`. Change the backend, run `make openapi`, commit all
  three. Never hand-edit either artifact, and never hand-merge one: take a side and
  regenerate.
- Declare every status code a route can return, with a `model` for anything that has a
  body. An undeclared code is invisible to the other half.
- `frontend/tests/api/contract.test.ts` runs against the mock handlers and against the
  real backend. Assert shapes, status codes and the `detail` a refusal carries — never
  seed data, which the two implementations differ on by design.

**Tests**

- **No test skips itself.** A test that needs a daemon or a browser goes in a tier
  declared in `backend/tests/tiers.py`. Everything else mirrors the source it covers. A
  skip exits 0 and looks like a pass, so `test_gate.py` fails on one.
- **The gate is `make pre-commit`, and there is nothing else.** No workflow ships, so a
  commit that skipped the hook was checked by nothing. New checks go in the gate.
- **The tiers are deliberately outside it** — `make test-e2e`, `make test-e2e-live` and
  `make db-test` each need something fetched or started first, and a gate that fetches a
  browser or a daemon is a gate people learn to commit around. They are the one tier nothing
  will tell you that you did not run: `test-e2e` after a UI change, `db-test` after touching
  `app/store/` or `deploy/`.
- **You have a browser. Use it.** After a UI change, run the app and look at it rather
  than inferring from the JSX. A screenshot is evidence; reading markup is a guess.

**Vendored and generated code — do not tidy**

- `frontend/src/components/ui/**`, `frontend/src/lib/utils.ts` and
  `frontend/public/mockServiceWorker.js` were written by a tool. You may change what they
  *do*; do not reformat them, strip their comments, or restyle them to match house rules.
- `openapi.json` and `frontend/src/api/schema.ts` are regenerate-only. A hand edit is a bug.
- `pnpm dlx shadcn add` does not put everything in `src/components/ui`. A block, a chart, a
  hook or a lib file lands in ordinary scanned source and will fail `make lint` in shadcn's
  idiom. Fix the values to tokens, or add that one path to `conformance.exclude` — never
  `conformance.allow`, which matches text rather than path.

**Frontend**

- **Never fetch in a `useEffect`.** Server state comes from TanStack Query hooks against
  `src/api/client.ts`. Effects are for synchronising with something outside React.
- **The theme is the only place a colour, a type size or a spacing step is defined.** A hex
  literal, a palette step like `bg-blue-500`, a `text-[13px]`, a `p-[7px]` or an opacity
  modifier like `bg-primary/80` fails `pnpm conformance`. Add a token to `src/index.css`.
- **Icon stroke comes from `--icon-stroke`**, not from the call site.
- **Never assume the app is served from `/`.** The worker registration, `src/api/base.ts`
  and the router basepath all derive from `import.meta.env.BASE_URL`.
- **Do not delete `src/mocks/`** when the backend feels real enough. It is the fixture
  layer every component test runs against.
- Use the `@/` alias for anything under `src/`. TypeScript is `strict`, plus
  `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes`.
- **There is no `.env.production`, on purpose.** Production mode loads no env file, so mock
  mode is off because nothing turns it on — do not add one, or you create the file in which
  someone can switch it back on. `public/` is copied into every build whatever the mode, so
  the `strip-mock-worker` plugin in `vite.config.ts` covers what no mode can; without it a
  production deploy ships `mockServiceWorker.js` to its own origin.
- The agent guard blocks writes to any `.env*` file. If one needs to change, say so and let
  a human edit it.

**Backend**

- **`app/wiring.py` is the only reader of the environment.** Everything below it takes its
  DSN, schema and migrate mode as arguments. Nothing degrades from Postgres to memory: a
  bad `DATABASE_URL` fails at boot.
- **Never widen the store's surface to take a tenant.** `Database.store(tenant_id)` is the
  only way to get one, and the tenant is a parameter on no method. A store you can hold is
  a store already scoped.
- **Every index on a tenant table leads with `tenant_id`**, or the policy's predicate
  cannot be satisfied and Postgres scans. Right answers, quietly slower, until the table is
  too large to fix cheaply.
- **Migrations are additive, and the application never applies them.** No `DROP COLUMN`, no
  `ALTER COLUMN ... TYPE`, no `RENAME`. `make migrate` applies; `make schema` and
  `make roles` regenerate the committed artifacts in `deploy/`.
- **The schema is data.** `app/store/ddl.py` holds one statement per four-digit key. Never
  edit a shipped entry; a column added later lands twice, once in its table's `CREATE` and
  once in the `0200_` repair band. Run `make schema` and commit.
- **The application must never see `DATABASE_OWNER_URL`.** It refuses to start if it can.
- **Do not delete `MemoryDatabase`** when Postgres feels real enough. The gate runs on it
  and must not need a daemon, and one implementation of a `Protocol` is a shape nothing
  checks.
- Seed rows are asserted in `tests/routes/test_tasks.py` and nowhere else. Memory hands out
  `"1"`, Postgres hands out uuids — pattern-match neither.

**Both halves**

- When a complexity gate fails, **split the function.** Raising a threshold or re-recording
  a baseline upward is a deliberate, reviewable decision, not the way to green. `make lint`
  lowers a stale baseline for you, and that change belongs in your commit.
- A **supply-chain cool-off** applies in both halves, so a package published in the last
  fortnight will not resolve — including via `pnpm dlx shadcn add`. Commit both lockfiles.

## Commands

```bash
make install        # both halves, then activate the versioned git hooks
make pre-commit     # the gate: lint-check + openapi-check + test
make dev            # both halves: backend :8000, frontend :5173, live mode
make dev-frontend   # frontend only, mock mode -- no backend required
make lint           # auto-fix and lint both halves (lint-check is the check-only variant)
make test           # both halves, then the contract suite against the real backend
make openapi        # regenerate the contract artifacts
make db             # a Postgres to develop against; prints the DATABASE_URL to export
make build          # production build of the frontend
```

The `Makefile` is the full list — the tiers, the migration targets and the per-half variants
as well. Each script under `devtools/` explains what it does and why, at the top.
`BACKEND_PORT`, `FRONTEND_PORT` and `PREVIEW_PORT` move every server, so a second checkout can
run at the same time.

Or work in one half with its own tools — `pnpm -C frontend test:watch`,
`pnpm -C frontend add some-package`, `cd backend && uv run pytest -q tests/routes/`,
`cd backend && uv add some-package`.

## Where to read more

| | |
|---|---|
| [CONTEXT.md](CONTEXT.md) | the glossary, and why renaming `tenant_id` is a day-one decision |
| [docs/deployment.md](docs/deployment.md) | serving the two halves, the release step, and two ways to ship something broken |
| [docs/installation.md](docs/installation.md) | prerequisites, and taking a later template update |
| [docs/adr/](docs/adr/) | the decisions, and [when to write one](docs/adr/README.md) |

This project was generated from
[agent-ready-fullstack-ts-python](https://github.com/CodeMocsh/agent-ready-fullstack-ts-python)
and can receive its later changes — see `docs/installation.md`. It ships
[Entire](https://entire.io) session hooks and an agent guard in `.claude/`; both are
optional; the guard says why it refused, in the refusal.

